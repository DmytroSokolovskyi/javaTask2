import {environment} from "../../environments/environment";

export const URL = {
  main: `${environment.URL_list}/`,
  save: `${environment.URL_list}/save`,
  update: `${environment.URL_list}/update/`,
  search: `${environment.URL_list}/search`,
  saveTodo: `${environment.URL_todo}/save/`,
  getTodo: `${environment.URL_todo}/`
}
